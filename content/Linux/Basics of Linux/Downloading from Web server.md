Using **wget** command...

In one terminal, start a web server (*python3 -m http.server*)
In second terminal, files can be copied from the server using **wget**
	wget http://10.10.233.17:8000/filename
	