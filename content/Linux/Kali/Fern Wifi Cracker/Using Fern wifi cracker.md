Once in the application with monitor mode enabled on the wlan (see [[Enable Wifi adapter]]), before we start scanning, we can double click next to "scan for access points".

This gives us this window, where we can select specific channels (if not all)
![[Pasted image 20241108223955.png]]

After selecting 'scan now' you will see that networks begin to be detected 
(412 in this example)
![[Pasted image 20241108224256.png]]
