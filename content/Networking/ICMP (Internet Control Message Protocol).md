Used for network diagnostics and error reporting.
Common commands:
	*ping* - uses ICMP to send an echo request (ICMP type 8) to test connection to target system and records RTT (round trip time)
		Can fail if target stems are offline, shutdown or firewall preventing.
	*traceroute* (tracert on windows) - Uses ICMP to discover route to host

